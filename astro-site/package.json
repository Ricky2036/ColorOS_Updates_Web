{
  "name": "os-archive-astro",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "node scripts/migrate-legacy.mjs",
    "dev": "astro dev",
    "build": "astro build && npm run budget",
    "preview": "astro preview",
    "check": "astro check",
    "budget": "node scripts/performance-budget.mjs",
    "test:content": "node scripts/verify-content.mjs",
    "test:browser": "node tests/browser-smoke.mjs",
    "verify": "npm run test:content && npm run check && npm run build && npm run test:browser"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^7.2.0",
    "sharp": "^0.35.3",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "puppeteer-core": "^25.4.0"
  }
}
